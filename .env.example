# Variáveis públicas (Vite injeta em tempo de build).
# Copie para .env local ou configure como Repository Variables no GitHub
# (Settings → Secrets and variables → Actions → Variables) para uso em CI.
# Os valores abaixo DEVEM ser apenas chaves públicas/publishable.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
